/**
 * Точка входа в приложение (entry point).
 * Подключает стили и инициализирует основную логику игры.
 */

// Импорт CSS-стилей для применения визуальных правил ко всей странице.
import "./css/style.css";

// Импорт основного класса игры, управляющего игровым процессом.
import Game from "./js/Game.js";

/**
 * Создаёт экземпляр игры.
 * @type {Game}
 */
const game = new Game();

/**
 * Запускает игровой процесс: инициализирует поле, сбрасывает счёт,
 * активирует обработку кликов и начинает таймер появления гоблинов.
 */
game.start();
